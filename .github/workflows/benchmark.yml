name: Benchmark

on:
  workflow_dispatch:
  pull_request:
    branches: [ main ]

jobs:

  parsers:

    name: Parser Benchmarks

    if: github.event_name == 'pull_request'

    runs-on: ubuntu-latest

    permissions:
      pull-requests: write

    steps:

    - name: Checkout
      uses: actions/checkout@v4

    - name: Download .NET SDK
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 10.0

    - name: Run parser benchmarks
      run: dotnet run -c Release -- --filter '*FlexibleParserBenchmark*' '*UltraHardenedParserBenchmark*'
      working-directory: Benchmarks

    - name: Upload results
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: parser-benchmark-results
        path: Benchmarks/BenchmarkDotNet.Artifacts/results/

    - name: Combine benchmark JSON results
      run: |
        jq -s '{ Benchmarks: [ .[].Benchmarks[] ] }' \
          Benchmarks/BenchmarkDotNet.Artifacts/results/*-report-full-compressed.json \
          > combined-benchmarks.json

    - name: Add allocation metrics to combined results
      run: |
        python3 -c "
        import json, glob
        files = glob.glob('Benchmarks/BenchmarkDotNet.Artifacts/results/*-report-full-compressed.json')
        alloc_entries = []
        for f in files:
            with open(f) as fh:
                data = json.load(fh)
            for b in data.get('Benchmarks', []):
                alloc = b.get('Memory', {}).get('BytesAllocatedPerOperation', 0)
                if alloc is not None:
                    alloc_entries.append({
                        'FullName': b['FullName'] + '.Allocated',
                        'Statistics': {'Mean': float(alloc), 'StandardDeviation': 0.0},
                        'Namespace': b.get('Namespace', ''),
                        'Type': b.get('Type', ''),
                        'Method': b.get('Method', '') + '_Allocated'
                    })
        with open('combined-benchmarks.json') as fh:
            combined = json.load(fh)
        combined['Benchmarks'].extend(alloc_entries)
        with open('combined-benchmarks.json', 'w') as fh:
            json.dump(combined, fh)
        "

    - name: Compare against baseline
      uses: benchmark-action/github-action-benchmark@4bdcce38c94cec68da58d012ac24b7b1155efe8b # v1
      with:
        tool: 'benchmarkdotnet'
        output-file-path: combined-benchmarks.json
        github-token: ${{ secrets.GITHUB_TOKEN }}
        gh-pages-branch: gh-pages
        benchmark-data-dir-path: benchmarks
        max-items-in-chart: 15
        alert-threshold: '115%'
        fail-on-alert: true
        comment-on-alert: true
        comment-always: true
        summary-always: true

  full:

    name: Full Benchmarks

    if: github.event_name == 'workflow_dispatch'

    runs-on: ubuntu-latest

    permissions:
      contents: write
      actions: write

    steps:

    - name: Checkout
      uses: actions/checkout@v4

    - name: Download .NET SDK
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 10.0

    - name: Run all benchmarks
      run: dotnet run -c Release -- --filter '*FlexibleParserBenchmark*' '*UltraHardenedParserBenchmark*'
      working-directory: Benchmarks

    - name: Upload results
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: full-benchmark-results
        path: Benchmarks/BenchmarkDotNet.Artifacts/results/

    - name: Combine benchmark JSON results
      run: |
        jq -s '{ Benchmarks: [ .[].Benchmarks[] ] }' \
          Benchmarks/BenchmarkDotNet.Artifacts/results/*-report-full-compressed.json \
          > combined-benchmarks.json

    - name: Add allocation metrics to combined results
      run: |
        python3 -c "
        import json, glob
        files = glob.glob('Benchmarks/BenchmarkDotNet.Artifacts/results/*-report-full-compressed.json')
        alloc_entries = []
        for f in files:
            with open(f) as fh:
                data = json.load(fh)
            for b in data.get('Benchmarks', []):
                alloc = b.get('Memory', {}).get('BytesAllocatedPerOperation', 0)
                if alloc is not None:
                    alloc_entries.append({
                        'FullName': b['FullName'] + '.Allocated',
                        'Statistics': {'Mean': float(alloc), 'StandardDeviation': 0.0},
                        'Namespace': b.get('Namespace', ''),
                        'Type': b.get('Type', ''),
                        'Method': b.get('Method', '') + '_Allocated'
                    })
        with open('combined-benchmarks.json') as fh:
            combined = json.load(fh)
        combined['Benchmarks'].extend(alloc_entries)
        with open('combined-benchmarks.json', 'w') as fh:
            json.dump(combined, fh)
        "

    - name: Compare and update baseline
      uses: benchmark-action/github-action-benchmark@4bdcce38c94cec68da58d012ac24b7b1155efe8b # v1
      with:
        tool: 'benchmarkdotnet'
        output-file-path: combined-benchmarks.json
        github-token: ${{ secrets.GITHUB_TOKEN }}
        auto-push: ${{ github.ref == 'refs/heads/main' }}
        gh-pages-branch: gh-pages
        benchmark-data-dir-path: benchmarks
        max-items-in-chart: 15
        alert-threshold: '115%'
        # This job publishes the baseline; it must not fail on noise (the FastConfig
        # runs only 1-3 iterations). Regressions are still surfaced via the summary.
        fail-on-alert: false
        comment-on-alert: true
        summary-always: true

    - name: Clean stale benchmark entries from gh-pages
      if: ${{ always() && github.ref == 'refs/heads/main' }}
      run: |
        git fetch origin gh-pages
        git worktree add /tmp/gh-pages gh-pages
        python3 -c "
        import json, pathlib, sys
        p = pathlib.Path('/tmp/gh-pages/benchmarks/data.js')
        if not p.exists():
            sys.exit(0)
        raw = p.read_text()
        prefix = 'window.BENCHMARK_DATA = '
        if not raw.startswith(prefix):
            sys.exit(0)
        data = json.loads(raw[len(prefix):])
        # Benchmark classes removed from the suite (precise prefixes so
        # UltraHardenedParserBenchmark is never matched).
        stale_prefixes = (
            'Benchmarks.HardenedParserBenchmark.',
            'Benchmarks.RequestSemanticsBenchmark.',
            'Benchmarks.AllSemanticChecksBenchmark.',
        )
        changed = False
        for key, entries in data.get('entries', {}).items():
            for entry in entries:
                before = len(entry.get('benches', []))
                entry['benches'] = [
                    b for b in entry.get('benches', [])
                    if not b.get('name', '').startswith(stale_prefixes)
                ]
                if len(entry['benches']) != before:
                    changed = True
        if not changed:
            print('No stale entries found.')
            sys.exit(0)
        p.write_text(prefix + json.dumps(data))
        print('Cleaned stale benchmark entries.')
        "
        cd /tmp/gh-pages
        git config user.name "github-actions[bot]"
        git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
        if git diff --quiet; then
          echo "No changes to commit."
        else
          git add benchmarks/data.js
          git commit -m "Clean stale benchmark entries from data.js"
          git push origin gh-pages
        fi
        cd -
        git worktree remove /tmp/gh-pages

    - name: Rebuild docs site
      if: ${{ always() && github.ref == 'refs/heads/main' }}
      run: gh workflow run "Deploy Docs to GitHub Pages"
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
